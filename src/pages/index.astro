---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="es">
    <head>
        <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
        <style>
            :root {
                --accent: #e0e0d1;
                --bg-light: #f8f9fa;
                --orb-color-solid: #f5f5dc; /* El color base de tu esfera */
                --glass: rgba(255, 255, 255, 0.4);
                --orb-gradient: radial-gradient(circle at 30% 30%, #fff, #f5f5dc, #d4d4b1);
            }

            body {
                background-color: var(--bg-light);
                color: #2d2d2d;
                overflow-x: hidden;
                /* El fondo inicial con gradiente de malla */
                background-image: 
                    radial-gradient(at 0% 0%, hsla(60,15%,90%,1) 0, transparent 50%), 
                    radial-gradient(at 100% 100%, hsla(200,10%,95%,1) 0, transparent 50%);
                transition: background-color 0.3s ease-out;
            }

            .hero-container {
                position: relative;
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                text-align: center;
                z-index: 1;
                padding: 20px;
            }

            h1 {
                font-size: clamp(3rem, 12vw, 7rem);
                font-weight: 800;
                letter-spacing: -0.04em;
                margin: 0;
                background: linear-gradient(180deg, #1a1a1a 30%, #666);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                filter: drop-shadow(0 10px 20px rgba(0,0,0,0.05));
            }

            .hero-subtitle {
                font-size: clamp(1rem, 3vw, 1.2rem);
                text-transform: uppercase;
                letter-spacing: 0.3em;
                color: #888;
                margin-top: 1rem;
            }

            .orb-wrapper {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                z-index: -1;
                pointer-events: none;
                width: 100%;
                display: flex;
                justify-content: center;
            }

            .light-orb {
                width: 300px;
                height: 300px;
                border-radius: 50%;
                background: var(--orb-gradient);
                box-shadow: 
                    0 0 80px rgba(245, 245, 220, 0.4),
                    inset -20px -20px 50px rgba(0,0,0,0.1);
                filter: blur(2px);
                transition: transform 0.1s ease-out;
                will-change: transform, filter;
            }

            .floating-section {
                position: relative;
                z-index: 10;
                padding: 100px 20px;
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                max-width: 1200px;
                margin: 0 auto;
                gap: 40px;
            }

            .card {
                background: var(--glass);
                backdrop-filter: blur(20px) saturate(180%);
                -webkit-backdrop-filter: blur(20px) saturate(180%);
                border: 1px solid rgba(255, 255, 255, 0.7);
                padding: 3rem 2rem;
                border-radius: 40px;
                transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
            }

            .card:hover {
                transform: translateY(-15px) scale(1.02);
                background: rgba(255, 255, 255, 0.6);
            }

            main {
                position: relative;
                z-index: 10;
                max-width: 800px;
                margin: 0 auto;
                padding: 100px 24px;
                font-size: 1.2rem;
                line-height: 1.8;
                text-align: center;
            }

            @media (max-width: 768px) {
                .light-orb { width: 200px; height: 200px; }
                .hero-container { padding-top: 150px; }
            }
        </style>
    </head>
    <body>
        <Header />
        
        <section class="hero-container">
            <div class="content-hero">
                <h1 id="title">¡Hola! Soy Leandro</h1>
                <p class="hero-subtitle">Bienvenidos a mi proyecto personal.</p>
            </div>

            <div class="orb-wrapper">
                <div class="light-orb" id="dynamic-orb"></div>
            </div>
        </section>

        <section class="floating-section">
            <div class="card">
                <h3>✨ Blablabla</h3>
                <p>Blablablabla.</p>
            </div>
            <div class="card">
                <h3>🌀 Blablabla</h3>
                <p>Blablablabla.</p>
            </div>
            <div class="card">
                <h3>🍃 Essence</h3>
                <p>blablablabla.</p>
            </div>
        </section>

        <main>
            <p>Página en construcción, poco a poco iré añadiendo todo para que esté mejor.</p>
            <div style="height: 30vh;"></div>
        </main>

        <Footer />

        <script>
            const orb = document.getElementById('dynamic-orb');
            const title = document.getElementById('title');
            const body = document.body;
            
            window.addEventListener('scroll', () => {
                const scrolled = window.scrollY;
                const val = scrolled * 0.5;
                
                // 1. Control de la Esfera
                if (orb) {
                    // La esfera crece más rápido para dar el efecto de "entrar"
                    const scale = 1 + scrolled * 0.005; 
                    orb.style.transform = `translate3d(0, ${val * 0.8}px, 0) scale(${scale})`;
                    orb.style.filter = `blur(${scrolled * 0.01}px) brightness(${1 + scrolled * 0.0005})`;
                }

                // 2. Control del Fondo (Efecto Inmersión)
                // Cuando llegas a 500px de scroll, el fondo es 100% el color de la bola
                const immersionOpacity = Math.min(scrolled / 500, 1);
                // Interpolamos de blanco/gris claro a crema (#f5f5dc)
                body.style.backgroundColor = `rgba(245, 245, 220, ${immersionOpacity})`;

                // 3. Control del Título
                if (title) {
                    title.style.transform = `translate3d(0, ${scrolled * 0.2}px, 0)`;
                    title.style.opacity = `${1 - scrolled / 500}`;
                }
            });
        </script>
    </body>
</html>